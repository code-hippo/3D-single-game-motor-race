/*
 * highscores.cpp
 *
 *  Created on: 09-Sep-2013
 *      Author: vignan_pc
 */

#include <sstream>
#include <string>
#include <stdlib.h>
using namespace std;

#include "highscores.h"
/*
ifstream myfile ("High_scores.txt");
string line;
//create temporary struct so you'll be able to fill the vector
my_struct temp;

vector <my_struct> players;//create array: my_struct players[10];

void scoreupdate(int inp_score){
	while(myfile.good()){
		getline (myfile,line);
		temp.score=atoi( line.substr(0, (int) line.find(' ')));
		temp.name=line.substr( (int) line.find(' ')+1);
		players.push_back(line);
	}
	myfile.close();
}
*/
